<div class="block-root">
  {{#if text}}
  <div class="relative">
    <p
      class="{{className}} {{#if error}}error-message{{/if}} {{#if success}}success-message{{/if}}"
    >
      {{text}}
    </p>
  </div>
  {{/if}}
</div>
